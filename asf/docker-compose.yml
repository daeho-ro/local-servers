services:
  asf:
    image: justarchi/archisteamfarm:latest
    container_name: asf
    restart: unless-stopped
    pull_policy: always
    volumes:
      - ./config:/app/config
    networks:
      - local

networks:
  local:
    external: true
